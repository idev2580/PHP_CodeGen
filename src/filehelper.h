#ifndef __CODEGEN_FILEHELPER__
#define __CODEGEN_FILEHELPER__

#include <stdlib.h>
#include <string.h>

const char* getFileExt(const char *s);
int getFilenameEidxWithoutEndingExt(const char *s);

#endif